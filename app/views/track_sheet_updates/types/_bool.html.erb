<% if prior.present? %>
  <%= render "track_sheet_updates/types/bool_with_value", name: name, value: prior %>
  &rarr;
<% end %>
<%= render "track_sheet_updates/types/bool_with_value", name: name, value: value %>
