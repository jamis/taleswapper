<div class="border rounded-lg px-2 py-1 flex flex-row gap-2 mb-2 <%= value ? "bg-blue-50" : "bg-gray-100 text-gray-400" %>">
  <span><%= name %></span>
  <% if prior != nil %>
    <span><%= prior ? '✅' : '❌' %></span>
    <span>&rarr;</span>
  <% end %>
  <span><%= value ? '✅' : '❌' %></span>
</div>
