<div class="border rounded-lg w-48 shadow-md flex flex-col overflow-hidden mb-2 <%= old ? 'bg-gray-100' : nil %>">
  <div class="border-b px-2 py-0 font-bold text-white <%= old ? 'bg-gray-500' : 'bg-blue-500' %>">
    <%= name %>
  </div>
  <div class="card-contents p-2 text-sm grow markdown h-full <%= old ? 'text-gray-500' : 'bg-blue-50' %>">
    <%=md value %>
  </div>
</div>
